language: generic

sudo : required
services:
  - docker

before_install:
  - docker build -t wojcikm/docker-react-multi -f ./client/Dockerfile.dev .

script:
  - docker run -e CI=true wojcikm/docker-react-multi npm run test -- --coverage
